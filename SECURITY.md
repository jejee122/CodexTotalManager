# Security Policy

## 当前支持状态

本仓库当前发布的是预生产候选源码，不承诺已经通过真实 Codex、真实 OAuth、
真实账号池或生产服务器的完整验收。

## 报告安全问题

请通过 GitHub Security Advisory 的私密报告入口提交安全问题。不要在公开
Issue、Discussion、论坛帖子或截图中粘贴以下内容：

- API Key、OAuth Token、Cookie、JWT、账号文件或 DPAPI 数据；
- SSH 私钥、VLESS/VMess/Trojan 链接、服务器地址或服务器身份；
- 包含请求正文、用户目录、账号、模型调用或运行路径的日志；
- ~/.codex、总管家运行数据目录或 v2rayN/Xray 配置。

报告时可以提供脱敏后的错误信息、复现步骤、受影响版本和文件哈希。

## 仓库数据边界

源码仓库不得包含运行数据、真实凭据、第三方可执行文件或本机专用配置。
CLIProxyAPI 只保留许可证、版本和固定 SHA-256 锁定信息；实际可执行文件必须
由发布流程单独注入并校验。

## 本机调试接口

皮肤功能使用本机回环 CDP。即使只监听 127.0.0.1，调试会话仍属于敏感本机
能力；启用期间不要同时运行不可信的本机程序，不使用皮肤时应恢复官方界面并
关闭对应调试会话。
